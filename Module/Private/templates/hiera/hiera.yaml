---
# Hiera module layer configuration (https://puppet.com/docs/puppet/6/hiera_config_yaml_5.html)
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Module default data"
    paths:
        - "common.yaml"
        - "%{::kernel}.yaml"
